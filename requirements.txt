# PTv3 inference environment validated on Linux x86_64, Python 3.8,
# PyTorch 2.1.0 and CUDA 12.1.  Install from the repository root with:
#   python -m pip install --no-build-isolation -r requirements.txt
#
# NVIDIA driver and the CUDA compiler/toolkit are system dependencies and
# cannot be installed by requirements.txt.  A CUDA 12.1-compatible driver is
# required; nvcc plus a C++ compiler are required to build local pointops.

--extra-index-url https://download.pytorch.org/whl/cu121
--find-links https://data.pyg.org/whl/torch-2.1.0+cu121.html

# CUDA/PyTorch runtime
torch==2.1.0+cu121
torchvision==0.16.0+cu121
torchaudio==2.1.0+cu121
spconv-cu120==2.3.6
flash-attn==2.5.5
torch-scatter==2.1.2+pt21cu121
torch-cluster==1.6.3+pt21cu121
torch-geometric==2.3.0
triton==2.1.0

# Point-cloud, LAS/LAZ and numerical runtime
numpy==1.24.4
scipy==1.10.1
laspy==2.5.4
lazrs==0.6.2
SharedArray==3.2.4
plyfile==1.0.3
trimesh==4.12.2
h5py==3.11.0
scikit-learn==1.3.2
pandas==2.0.3

# Pointcept configuration/model utilities
addict==2.4.0
einops==0.8.1
PyYAML==6.0.3
yapf==0.43.0
# Pointcept Config uses the third-party regex module on Windows.
regex==2024.11.6
termcolor==2.4.0
tqdm==4.67.1
timm==1.0.29
tensorboard==2.14.0
tensorboardX==2.6.2.2
packaging==26.2
ninja==1.13.2

# Local CUDA extension imported by the full Pointcept model registry.
./libs/pointops
